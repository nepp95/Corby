on: [push]

jobs:
  hercules-charts:
    runs-on: ubuntu-latest
    name: Charts generated by src-d/hercules
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Hercules
        uses: nepp95/hercules@master
      - uses: actions/upload-artifact@master
        with:
          name: hercules_charts
          path: hercules_charts.tar
