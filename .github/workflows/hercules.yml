on: [push]
jobs:
  hercules-charts:
    runs-on: ubuntu-latest
    name: Charts generated by src-d/hercules
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 0
      - uses: docker://srcd/hercules:latest
        with:
          args:
            "pip install 'matplotlib==3.2.0' && hercules --devs --pb . | labours -m all -f pb --disable-projector -o hercules_charts && cd hercules_charts && tar -cf ../hercules_charts.tar * ../hercules_charts_* && cd .. && rm -r hercules_charts"
      #- name: Hercules
      #  uses: src-d/hercules@master
      - uses: actions/upload-artifact@master
        with:
          name: hercules_charts
          path: hercules_charts.tar
